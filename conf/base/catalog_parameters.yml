# yaml file for parameters, each pipeline that will be exposed to streamlit should have it's own catalog entry,

parameters__test:
  type: PartitionedDataSet
  dataset:
    type: yaml.YAMLDataSet
  path: data/00_parameters/test/
  filename_suffix: .yml
